From b0aa0674800ac4cc67f0ae87d4fd0baa058cda1f Mon Sep 17 00:00:00 2001
From: Morgan Jones <me@numin.it>
Date: Sat, 25 Jan 2025 18:21:22 -0800
Subject: [PATCH] {constants,menu,header}: Add nixpkgs gold support

---
 .../product_branding_free_edition.tsx         |   5 +-
 .../components/header_footer_route/header.tsx |   5 +-
 .../menu/menu_items/menu_start_trial.tsx      |  41 ++---
 webapp/channels/src/utils/constants.tsx       | 149 ++++++++++++++++++
 webapp/channels/webpack.config.js             |   7 +
 5 files changed, 175 insertions(+), 32 deletions(-)

diff --git a/webapp/channels/src/components/global_header/left_controls/product_menu/product_branding_team_edition/product_branding_free_edition.tsx b/webapp/channels/src/components/global_header/left_controls/product_menu/product_branding_team_edition/product_branding_free_edition.tsx
index 93cb8ab263..2f2946d4e0 100644
--- a/webapp/channels/src/components/global_header/left_controls/product_menu/product_branding_team_edition/product_branding_free_edition.tsx
+++ b/webapp/channels/src/components/global_header/left_controls/product_menu/product_branding_team_edition/product_branding_free_edition.tsx
@@ -8,6 +8,7 @@ import styled from 'styled-components';
 import {getLicense} from 'mattermost-redux/selectors/entities/general';
 
 import Logo from 'components/common/svg_images_components/logo_dark_blue_svg';
+import {getEdition, getEditionColor} from 'utils/constants';
 
 import {LicenseSkus} from 'utils/constants';
 
@@ -32,7 +33,7 @@ const Badge = styled.span`
     padding: 2px 6px;
     border-radius: var(--radius-s);
     margin-left: 12px;
-    background: rgba(var(--sidebar-text-rgb), 0.08);
+    background: ${getEditionColor()};
     color: rgba(var(--sidebar-text-rgb), 0.75);
     font-family: 'Open Sans', sans-serif;
     font-size: 10px;
@@ -57,7 +58,7 @@ const ProductBrandingFreeEdition = (): JSX.Element => {
                 width={116}
                 height={20}
             />
-            <Badge>{badgeText}</Badge>
+            <Badge>{getEdition()}</Badge>
         </ProductBrandingFreeEditionContainer>
     );
 };
diff --git a/webapp/channels/src/components/header_footer_route/header.tsx b/webapp/channels/src/components/header_footer_route/header.tsx
index 4db9dce146..3cf1e82fd2 100644
--- a/webapp/channels/src/components/header_footer_route/header.tsx
+++ b/webapp/channels/src/components/header_footer_route/header.tsx
@@ -10,6 +10,7 @@ import {getConfig, getLicense} from 'mattermost-redux/selectors/entities/general
 
 import BackButton from 'components/common/back_button';
 import Logo from 'components/common/svg_images_components/logo_dark_blue_svg';
+import {getEdition, getEditionColor} from 'utils/constants';
 
 import './header.scss';
 import {LicenseSkus} from 'utils/constants';
@@ -28,9 +29,7 @@ const Header = ({alternateLink, backButtonURL, onBackButtonClick}: HeaderProps)
 
     let freeBanner = null;
     if (license.IsLicensed === 'false') {
-        freeBanner = <><Logo/><span className='freeBadge'>{'TEAM EDITION'}</span></>;
-    } else if (license.SkuShortName === LicenseSkus.Entry) {
-        freeBanner = <><Logo/><span className='freeBadge'>{'ENTRY EDITION'}</span></>;
+        freeBanner = <><Logo/><span className='freeBadge' style={{background: `${getEditionColor()} !important`}}>{getEdition()}</span></>;
     }
 
     let title: React.ReactNode = SiteName;
diff --git a/webapp/channels/src/components/widgets/menu/menu_items/menu_start_trial.tsx b/webapp/channels/src/components/widgets/menu/menu_items/menu_start_trial.tsx
index d193e5e202..74a6134fc2 100644
--- a/webapp/channels/src/components/widgets/menu/menu_items/menu_start_trial.tsx
+++ b/webapp/channels/src/components/widgets/menu/menu_items/menu_start_trial.tsx
@@ -10,7 +10,7 @@ import {getLicense} from 'mattermost-redux/selectors/entities/general';
 
 import ExternalLink from 'components/external_link';
 
-import {LicenseLinks, LicenseSkus} from 'utils/constants';
+import {getEdition, getEditionName, getEditionMessage, getEditionLink, getEditionColor} from 'utils/constants';
 
 import './menu_item.scss';
 
@@ -21,7 +21,7 @@ const FreeVersionBadge = styled.div`
      padding: 2px 6px;
      border-radius: var(--radius-s);
      margin-bottom: 6px;
-     background: rgba(var(--center-channel-color-rgb), 0.08);
+     background: ${getEditionColor()};
      color: rgba(var(--center-channel-color-rgb), 0.75);
      font-family: 'Open Sans', sans-serif;
      font-size: 10px;
@@ -56,41 +56,28 @@ const MenuStartTrial = (props: Props): JSX.Element | null => {
             role='menuitem'
             id={props.id}
         >
-            <FreeVersionBadge>{badgeText}</FreeVersionBadge>
+            <FreeVersionBadge>{getEdition()}</FreeVersionBadge>
             <div className='editionText'>
-                {isEntryLicense ? (
+                {
                     formatMessage({
-                        id: 'navbar_dropdown.entryVersionText',
-                        defaultMessage: 'Entry offers Enterprise Advance capabilities <link>with limits</link> designed to support evaluation.',
+                        // Have to set to something that doesn't exist to avoid i18n
+                        id: 'navbar_dropdown.nixVersionText',
+                        defaultMessage: `This is the <link>unknown</link> edition of Mattermost.<message></message>`,
                     },
                     {
+                        message: (msg: React.ReactNode) => (
+                            <em>{getEditionMessage()}</em>
+                        ),
                         link: (msg: React.ReactNode) => (
                             <ExternalLink
-                                location='menu_start_trial.entry-link'
-                                href={LicenseLinks.ENTRY_LIMITS_INFO}
+                                location='menu_start_trial.unsupported-link'
+                                href={getEditionLink()}
                             >
-                                {msg}
+                            {getEditionName()}
                             </ExternalLink>
                         ),
                     })
-                ) : (
-                    formatMessage(
-                        {
-                            id: 'navbar_dropdown.versionText',
-                            defaultMessage: 'This is the free <link>unsupported</link> edition of Mattermost.',
-                        },
-                        {
-                            link: (msg: React.ReactNode) => (
-                                <ExternalLink
-                                    location='menu_start_trial.unsupported-link'
-                                    href={LicenseLinks.UNSUPPORTED}
-                                >
-                                    {msg}
-                                </ExternalLink>
-                            ),
-                        },
-                    )
-                )}
+                }
             </div>
         </li>
     );
diff --git a/webapp/channels/src/utils/constants.tsx b/webapp/channels/src/utils/constants.tsx
index da25f5ebee..d23995b9a9 100644
--- a/webapp/channels/src/utils/constants.tsx
+++ b/webapp/channels/src/utils/constants.tsx
@@ -1108,8 +1108,157 @@ export const LicenseLinks = {
     SOFTWARE_SERVICES_LICENSE_AGREEMENT_TEXT: 'Software Services and License Agreement',
     UNSUPPORTED: 'https://mattermost.com/pricing/',
     UNSUPPORTED_UPGRADE_LINK: 'https://docs.mattermost.com/administration-guide/upgrade/enterprise-install-upgrade.html#upgrading-to-enterprise-edition-from-mattermost-team-edition',
+
+    // nixpkgs gold license links
+    SUPPORTED_WITH_NIXPKGS_GOLD: 'https://github.com/Jaculabilis/nixpkgs-gold',
+    SUPPORTED_WITH_NIXPKGS_GOLD_TRIAL: 'https://nixos.org/donate/',
+    SUPPORTED_WITH_NIXOS: 'https://nixos.org',
 };
 
+/**
+ * Valid build types.
+ */
+enum GoldBuildType {
+    GOLD = 'gold',
+    GOLD_TRIAL = 'gold-trial',
+    NIX = 'nix',
+    NONE = 'none'
+}
+
+// Pull the nix env out of the window variable. Normally this is provided by webpack.
+const nixEnv: Record<string, string|number|boolean|null> = Object.freeze(window.nixEnv || {});
+
+/**
+ * Returns the current nixpkgs gold build type.
+ * @returns GoldBuildType
+ */
+function getBuildType(): GoldBuildType {
+    if (parseInt(nixEnv.NIXPKGS_GOLD)) {
+        if (parseInt(nixEnv.NIXPKGS_GOLD_LICENSED)) {
+            return GoldBuildType.GOLD;
+        } else {
+            return GoldBuildType.GOLD_TRIAL;
+        }
+    } else if (nixEnv.NIX_STORE) {
+        return GoldBuildType.NIX;
+    } else {
+        return GoldBuildType.NONE;
+    }
+}
+
+/**
+ * Nixpkgs gold ads. This comes in as a newline separated string, split it and cache it.
+ */
+const NIXPKGS_GOLD_ADS: string[] = `${(nixEnv.NIXPKGS_GOLD_ADS || '')}`.trim().split("\n").map(ad => {
+    return ad.replace(/\$out/, nixEnv.out || '$out')
+});
+
+/**
+ * Gets the nixpkgs gold trial message.
+ * @param intervalMs the interval for changing messages
+ * @returns string
+ */
+let lastMessage_: string = '';
+let lastTime_: Time|null = null;
+function getGoldTrialMessage(intervalMs: number = 30 * 1000): string {
+    // Ratelimit how often nixpkgs gold(tm) messages change
+    const now = new Date();
+    if (!lastTime_ || now.getTime() > lastTime_.getTime() + intervalMs) {
+        lastMessage_ = NIXPKGS_GOLD_ADS[Math.floor(Math.random() * NIXPKGS_GOLD_ADS.length)] || nixEnv.NIXPKGS_GOLD_AD || '';
+        lastTime_ = now;
+    }
+
+    return lastMessage_;
+}
+
+/**
+ * Gets the nixpkgs gold edition.
+ * @returns string
+ */
+export function getEdition(): string {
+    switch (getBuildType()) {
+        case GoldBuildType.GOLD:
+            return 'GOLD EDITION';
+        case GoldBuildType.GOLD_TRIAL:
+            return 'GOLD TRIAL EDITION';
+        case GoldBuildType.NIX:
+            return 'NIX EDITION';
+        default:
+            return 'FREE EDITION';
+    }
+}
+
+/**
+ * Gets the nixpkgs gold edition name.
+ * @returns string
+ */
+export function getEditionName(): string {
+    switch (getBuildType()) {
+        case GoldBuildType.GOLD:
+            return 'nixpkgs gold';
+        case GoldBuildType.GOLD_TRIAL:
+            return 'nixpkgs gold free trial';
+        case GoldBuildType.NIX:
+            return 'nixpkgs';
+        default:
+            return 'unsupported';
+    }
+}
+
+/**
+ * Gets the link for the nixpkgs gold edition.
+ * @returns string
+ */
+export function getEditionLink(): string {
+    switch (getBuildType()) {
+        case GoldBuildType.GOLD:
+            return LicenseLinks.SUPPORTED_WITH_NIXPKGS_GOLD;
+        case GoldBuildType.GOLD_TRIAL:
+            return LicenseLinks.SUPPORTED_WITH_NIXPKGS_GOLD_TRIAL;
+        case GoldBuildType.NIX:
+            return LicenseLinks.SUPPORTED_WITH_NIXOS;
+        default:
+            return LicenseLinks.UNSUPPORTED;
+    }
+}
+
+/**
+ * Gets the nixpkgs gold edition message.
+ * @returns string
+ */
+export function getEditionMessage(): string {
+    switch (getBuildType()) {
+        case GoldBuildType.GOLD:
+        case GoldBuildType.NIX: {
+            if (nixEnv.out) {
+                return ` Proudly hosted at ${nixEnv.out}`;
+            } else {
+                return '';
+            }
+        }
+        case GoldBuildType.GOLD_TRIAL:
+            return ' ' + getGoldTrialMessage();
+        default:
+            return '';
+    }
+}
+
+/**
+ * Gets a color for the edition theme.
+ * @returns rgba CSS value
+ */
+export function getEditionColor(): string {
+    switch (getBuildType()) {
+        case GoldBuildType.GOLD:
+        case GoldBuildType.GOLD_TRIAL:
+            return 'rgba(var(--away-indicator-rgb), 0.3)';
+        case GoldBuildType.NIX:
+            return 'rgba(var(--dnd-indicator-rgb), 0.3)';
+        default:
+            return 'rgba(var(--sidebar-text-rgb), 0.08)';
+    }
+}
+
 export const MattermostLink = 'https://mattermost.com/';
 
 export const BillingSchemes = {
diff --git a/webapp/channels/webpack.config.js b/webapp/channels/webpack.config.js
index 852046de70..a6d104868a 100644
--- a/webapp/channels/webpack.config.js
+++ b/webapp/channels/webpack.config.js
@@ -432,8 +432,15 @@ if (DEV) {
     env.NODE_ENV = JSON.stringify('production');
 }
 
+// Allow certain Nix variables into the build
+const nixEnv = Object.fromEntries(
+    Object.entries(process.env)
+        .filter(([key, val]) => key.match(/(^NIXPKGS_|^NIX_STORE$|^out$)/))
+        .map(([key, val]) => [key, JSON.stringify(val)])
+);
 config.plugins.push(new webpack.DefinePlugin({
     'process.env': env,
+    'window.nixEnv': nixEnv,
 }));
 
 if (targetIsDevServer) {
-- 
2.53.0

